
# set app variables
env BUILD_COMMIT_TAG;  # import from Dockerfile
env BUILD_COMMIT_SHA;  # import from Dockerfile

# set environment variables
env JaegerCollector_Host;
env JaegerCollector_OTLPHttpPort;

# service identity（預設值由 config.lua 提供，在此宣告讓 Lua 可透過 os.getenv 讀取）
env SERVICE_NAME;

# local
